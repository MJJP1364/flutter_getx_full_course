import 'package:flutter/material.dart';

class GetxControllerExzmple extends StatelessWidget {
  const GetxControllerExzmple({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('GetX Controller Example'),
      ),
    );
  }
}
